
        // var socket = new Socket(AddressFamily.InterNetwork, SocketType.Raw, ProtocolType.Udp);
        // IPAddress ubuntu = IPAddress.Parse("192.168.0.39");
        // IPEndPoint bindEndpoint = new IPEndPoint(ubuntu, 0);
        // socket.Bind(bindEndpoint);
        
        // IPAddress any = IPAddress.Parse("0.0.0.0");
        // IPEndPoint rcvEndpoint = new IPEndPoint(any, 0);
        
        // byte[] receiveBuffer = new byte[1024];
        // await socket.ReceiveFromAsync(receiveBuffer, rcvEndpoint);
        // Console.WriteLine(BitConverter.ToString(receiveBuffer));
